<Page x:Class="ClanBattleGame.View.Pages.BattlePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClanBattleGame.View.Pages"
      xmlns:control="clr-namespace:ClanBattleGame.View.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="10*"/>
            </Grid.RowDefinitions>

            <control:HexField Grid.Row="1" DataContext="{Binding Field}" />

            <StackPanel 
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <TextBlock Text="{Binding ClanA.Name}" Margin="10"/>
                <ProgressBar Width="150" Margin="0 10 10 10"
                             Maximum="{Binding ClanATotalHP, Mode=OneWay}"
                             Value="{Binding ClanACurrentHP, Mode=OneWay}"/>
            </StackPanel>
                
            <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="Усього ходів:"/>
            <TextBlock Text=">>>>>"/>
            </StackPanel>

            <StackPanel
                VerticalAlignment="Center"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <ProgressBar Width="150" Margin="10 10 0 10"
                             Maximum="{Binding ClanBTotalHP, Mode=OneWay}"
                             Value="{Binding ClanBCurrentHP, Mode=OneWay}"/>
                <TextBlock Text="{Binding ClanB.Name}" Margin="10"/>
            </StackPanel>
        </Grid>
        
        
        <Border Background="AliceBlue" HorizontalAlignment="Right" Width="300" Grid.Column="1">
        <ScrollViewer  HorizontalAlignment="Right"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollBarVisibility="Disabled"
              Margin="5">
            <TextBlock Text="{Binding BattleLog}"
           TextWrapping="Wrap"
           FontFamily="Consolas"
           FontSize="10" Width="300"/>
        </ScrollViewer>
        </Border>

        <Border Background="AliceBlue" Grid.ColumnSpan="2" HorizontalAlignment="Left" Width="300" Grid.Column="0">
            <ScrollViewer  HorizontalAlignment="Right"
      VerticalScrollBarVisibility="Auto"
      HorizontalScrollBarVisibility="Disabled"
      Margin="5">
                <TextBlock Text="{Binding BattleLog}"
   TextWrapping="Wrap"
   FontFamily="Consolas"
   FontSize="10" Width="300"/>
            </ScrollViewer>
        </Border>
    </Grid>

</Page>
